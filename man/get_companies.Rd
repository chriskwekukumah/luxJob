% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_compagnie_detail.R, R/get_compagnies.R,
%   R/get_companies_limit100.R
\name{get_companies}
\alias{get_companies}
\title{Get a book by its ID}
\usage{
get_companies(limit = 100)

get_companies(limit = 100)

get_companies(limit = 100)
}
\arguments{
\item{book_id}{The unique identifier of the book.}
}
\value{
A data frame with columns: book_id, title, author, skill_id. Returns NULL if no book is found.

A data frame with company data (up to 100 rows).

A data frame with columns: book_id, title, author, skill_id. Returns NULL if no book is found.
}
\description{
Retrieves the first 100 companies from the \code{adem.companies} table.
Automatically establishes and closes the database connection.
}
\details{
This is a convenience function for quick access to company data during
development or exploration. Uses \code{connect_db()} internally and always
closes the connection properly.
}
\examples{
companies <- get_companies()
head(companies)
nrow(companies)

}
